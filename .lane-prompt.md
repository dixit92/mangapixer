# Lane 3 - Update All Libraries (1.8.0, backend + small admin UI)

Work ONLY in this worktree: `D:\dev\lp-mangaplex.update-all-libraries` (branch `feature/update-all-libraries`,
based on `dev` @ d243a06). Never touch the integrator checkout or another worktree. No git remote; commit
locally to this lane branch only (no push).

## Scope
Add a single admin action to trigger a scan/update of ALL libraries at once ("now that library updates are
quick, one button to update all in the Administration tab"). Today only a per-library scan exists:
`AdminController.cs` `TriggerScan` = `POST libraries/{id}/scan` (~line 230), which uses `LibraryScanPolicy` +
`ScanRunRegistry` (both already injected) and guards against a scan already running.

Implement:
1. **New endpoint** (e.g. `POST libraries/scan-all`) on `AdminController` that enqueues a scan for every
   registered library, reusing the existing scan trigger path + `LibraryScanPolicy`/`ScanRunRegistry`. Respect
   the scan-in-progress model per library (skip/queue libraries already scanning rather than erroring the whole
   batch). Return a summary (how many started / skipped). Keep it consistent with the existing scan semantics.
2. **Admin UI button** in the Administration/libraries screen that calls it, with basic feedback (started/skipped
   counts). Wire the API method in the frontend api service.

## Files you own
`src/MangaPlex.Server/Features/Admin/AdminController.cs` (KNOWN hot file - you are its sole owner this cycle) +
the admin frontend component/service for the button. No other lane touches these. Do NOT touch
`CatalogBrowseService.cs`, `library-browse.component.ts`, or `reader.component.ts`.

## Verify
- Backend: `sdk:10.0` container - `dotnet build MangaPlex.slnx -c Release && dotnet test MangaPlex.slnx --no-build -c Release`. If the endpoint changes the API surface, regenerate/verify `contracts/openapi.json`.
- Web: `node:24` container - `npm ci && npm run lint && npm run build && npm run test:ci`.
- Add at least one test through the public surface (WebApplicationFactory HTTP test) per the repo's wiring rule.
- Report tests by kind (unit / service-with-DB / HTTP / browser). Two KNOWN flakes exist (audit_events "already
  exists", ScanCancellation) - they pass in isolation; do NOT chase them (Lane 5 owns them).

## Hand-off
- Commit to `feature/update-all-libraries`; end with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Write your OWN lane note at `C:\Users\earth\OneDrive\Obsidian\Dev\MangaPlex\Features\MangaPlex - Update All Libraries.md`.
  ASCII hyphens only; escape wikilink pipes in tables as `\|`.
- Do NOT write the hub, backlog, cycle note, Feature Index, or Release Index (integrator-owned). Only your lane note.
- If you spawn any journaling subagent, PASS IT the constraint: write ONLY this lane's own note, never the
  hub/backlog/cycle/index notes.
- Report the final branch head commit. Verify you actually COMMITTED + JOURNALED (not just explored) before reporting done.
