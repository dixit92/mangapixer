# Lane 5 - Test Flake Stabilization (1.8.0, backend test infra)

Work ONLY in this worktree: `D:\dev\lp-mangaplex.flake-stabilization` (branch `feature/flake-stabilization`,
based on `dev` @ d243a06). Never touch the integrator checkout or another worktree. No git remote; commit
locally to this lane branch only (no push).

## Scope
Two pre-existing flakes have been carried across several cycles (they pass in isolation, so they have been
logged-not-fixed each time). Retire them:
1. **`OpenApiSnapshot` / `HostingCorrectness` "audit_events already exists".** A parallel host-boot DB-init race:
   multiple `WebApplicationFactory`/host boots race on creating the `audit_events` table (migration/EnsureCreated
   or a startup hosted service) when tests run in parallel. Find the shared test-host/bootstrap path and make DB
   init idempotent/serialized per test DB (e.g. guard table creation, isolate each test's SQLite DB, or serialize
   the init) so parallel boots cannot collide.
2. **`ScanCancellation_CancelsCooperatively`.** Determine why it is timing-sensitive and make it deterministic
   (await the right signal instead of racing a timing window; do not just add sleeps).

Investigate root cause first; fix deterministically. Run the full suite in PARALLEL (the default) several times
to prove the flakes are gone - a fix that only passes in isolation has not fixed the flake.

## Files you own
The shared test-host / bootstrap fixtures (`tests/MangaPlex.TestSupport/**` and the specific test files/classes
for `OpenApiSnapshot`, `HostingCorrectness`, `ScanCancellation`). You MAY make a minimal, justified change to app
startup/DB-init if that is the genuine root cause (note it carefully). Do NOT touch the import test area (Lane 4),
`AdminController.cs` (Lane 3), or the reader/browse components (Lanes 1/2).

## Verify
- `sdk:10.0` container: `dotnet build MangaPlex.slnx -c Release && dotnet test MangaPlex.slnx --no-build -c Release`,
  install p7zip-full. Run the FULL suite (parallel) multiple times (e.g. 3-5 runs) to demonstrate the flakes no
  longer reproduce. Report tests by kind (unit / service-with-DB / HTTP / process) and the repeat-run results.
- Baselines: Core 156 / MediaWorker 36 / Server 497 (1.7.3).

## Hand-off
- Commit to `feature/flake-stabilization`; end with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Write your OWN lane note at `C:\Users\earth\OneDrive\Obsidian\Dev\MangaPlex\Features\Lanes\1.8.0\MangaPlex - Test Flake Stabilization.md`
  (root cause of each flake + the fix + repeat-run evidence). ASCII hyphens only; escape wikilink pipes as `\|`.
- Do NOT write the hub, backlog, cycle note, Feature Index, or Release Index. Only your lane note.
- If you spawn any journaling subagent, PASS IT the constraint: write ONLY this lane's own note, never the
  hub/backlog/cycle/index notes.
- Report the final branch head commit. Verify you actually COMMITTED + JOURNALED (not just explored) before reporting done.
