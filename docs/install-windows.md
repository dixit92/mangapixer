# Install on Windows

A native Windows package (a tray app and an MSI installer) is in development
and is not part of this release yet. Until it ships, run the server on
Windows with Docker Desktop by following [Install with Docker](install-docker.md).

<!-- TODO(windows-cycle): replace this stub once the tray app and MSI installer ship. Planned sections: -->
<!-- TODO(windows-cycle): system requirements (Windows version, architecture, whether a .NET runtime is bundled). -->
<!-- TODO(windows-cycle): installing with the MSI - install location, silent-install switches, upgrade and uninstall behaviour. -->
<!-- TODO(windows-cycle): the tray app - start/stop, open in browser, view logs, run at sign-in. -->
<!-- TODO(windows-cycle): where data, cache, scratch, logs and backups live on Windows (defaults and how to change them). -->
<!-- TODO(windows-cycle): port and firewall defaults, and how to change the listening URL. -->
<!-- TODO(windows-cycle): pointing libraries at local folders and network shares (read-only access, service account permissions). -->
<!-- TODO(windows-cycle): Data Protection keys encrypted with DPAPI on Windows and what that means for moving an install to another PC. -->
<!-- TODO(windows-cycle): first-run setup and upgrade notes specific to the Windows package. -->
